#include <stdio.h>
#include <stdlib.h>

/**
 * Author: ANEHI GODWIN OHINOYI
 * Program: WinMingle Community C Training
 * Description: Program the print set 8f number from 0-9 (decimal-binary) 
 */

/* entry point for the program */
int main(void)

/* function for assigning a variable of character data type */
    char x = '0';
    /* function for while loop */
    while (x <= '9')
    {
	    /* putchar command for executing statement
	    putchar(x);
	    */
	    /* function for continious sum for condition */
            x++;
    }

/* function for executing a new line */
    putchar('\n');

    /* for returning the value of the program */
	return (0);
}
